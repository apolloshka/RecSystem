08.03
**Цель диплома:** Построить систему рекомендаций сообществ ВК на основе схожести пользователей по подпискам и сравнить несколько алгоритмов ~~(Popularity / UserKNN / ItemKNN / MF)~~ с offline-оценкой на подграфе подписок.



- **Чёткая формулировка задачи** (что рекомендуем? кому? в каком контексте?).
    
- **Данные** (как получены, как очищены, какие ограничения/этика/приватность).
    
- **Базовые модели** (не один алгоритм, а сравнение + улучшение).
    
- **Метрики и протокол экспериментов** (offline-оценка, разбиение по времени, статистика).
    
- **Интерпретация результатов и ограничения** (что работает, где ломается, что можно улучшить).


ГЛОССАРИЙ
- РЕКОМЕНДАТЕЛЬНАЯ СИСТЕМА
- КОЛЛАБОРАТИВНАЯ ФИЛЬТРАЦИЯ
- user-based 
- item-based
- API
- access token
- 

Теория

Основные алгоритмы рекомендательных систем.

1. Системы коллаборативной фильтрации (collaborative filtering)

Основная идея таких систем состоит в том, что если пользователи имели одинаковые интересы в прошлом, то в будущем их предпочтения также будут совпадать. В качестве примера рассмотрим книжный интернет-магазин. Пусть в прошлом истории покупок пользователей А и Б в данном магазине сильно пересекались. При появлении пользователя А на сайте мы хотим предложить ему новую книгу, которую он еще не читал, а пользователь Б, как раз, недавно приобрел книгу, которую А еще не видел. В такой ситуации будет разумно предложить пользователю А прочитать ее. Выше описан принцип работы класса алгоритмов коллаборативной фильтрации, которые в англоязычной литературе называются **user-based**, то есть основанные на статистике о пользователях. Как видно из примера, мы сравниваем похожесть между пользователями, основываясь на рейтингах оцененных объектов.
В то же время, почему бы нам не использовать ту же статистику для сравнения продуктов между собой, а потом сопоставить результаты двух подходов. Такой метод, основанный на сравнение схожести объектов, называется **item-based**. Здесь основная идея состоит в том, что если пользователям, которые оценили два продукта, понравились оба, то пользователям, которые попробовали только один, можно предлагать второй, который вероятнее всего, им понравится. 

2. Системы фильтрации на основе содержания (content-based filtering)

Этот тип систем основан на наличие информации об описании и профиле, состоящем из набора характеристик элемента. Если снова рассмотреть пример книжного магазина, то в качестве характеристик можно взять жанр, тему или автора книги. Затем для каждого пользователя создается профиль путем присвоения характеристик сходных с характеристиками элементов, исходя из анализа его поведения в прошлом, либо явно спрашивая о его предпочтениях. Далее пользователю рекомендуются объекты, похожие на те, которые этот пользователь уже употребил, либо указал как предпочтительные. Похожести оцениваются по признакам содержимого объектов. В примере с книжным магазином система могла определить, что автору нравятся детективы и новеллы определенных авторов, и в последствие рекомендовать книги этих жанров или авторов.

3. Системы, основанные на знаниях (knowledge-based filtering)

Рекомендации, основанные на знаниях, используются обычно в таких областях, как электроника, где покупатели совершают покупки раз в пару лет, так как в данной области мы не можем положиться на историю покупок, которая используется в качестве входных данных для методов коллаборативной фильтрации и методов, основанных на содержании. Рассмотрим для примера рекомендательную систему, которая помогает пользователя выбрать фотокамеру. Обычный пользователей покупает новую камеру только один раз в несколько лет. Таким образом, рекомендательная система не может построить профиль пользователя или предложить камеры, которые понравились другим пользователям, так как в противном случае предлагаться будут только бестселлеры. Поэтому алгоритмы, основанные на знаниях, обычно используют дополнительные данные, как о пользователях, так и о самих продуктах, для формирования списка рекомендаций.

4. Гибридные системы (hybrid filtering)

Каждый из вышеописанных методов имеет свои преимущества и недостатки в зависимости от поставленной задачи. Достаточно очевидным решением всех этих проблем является объединение различных подходов для того, чтобы обеспечить большую точность рекомендаций. О том, как измерять точность рекомендаций, мы поговорим в разделе о способах оценки качества рекомендаций. Если, например, у нас есть данные об описании продуктов, профиль пользователей и история его покупок, то мы можем улучшить рекомендательную систему путем объединения методов коллаборативной фильтрации и алгоритмов фильтрации по содержанию. Таким образом, в случае появления нового пользователя в системе, о котором нет истории покупок, мы сможем использовать рекомендации на основе алгоритмов фильтрации по содержанию, а в случае большого объема статистических данных строить более точный прогноз, используя методы коллаборативной фильтрации.


http://intsysjournal.ru/pdfs/21-4/18-30-Kutanin.pdf



Рекомендательная система анализирует поведение пользователя в прошлом — просмотры, клики, лайки, таймспент и другие сигналы, чтобы предсказать, какой контент с наибольшей вероятностью заинтересует его в будущем.

Первые рекомендательные системы появились в мире еще в конце 1990-х и расцвели в 2010-х. А сегодня сложно представить музыкальный или видеосервис без персональных рекомендаций. Эти технологии, основанные на машинном обучении и искусственном интеллекте, подбирают контент для каждого пользователя. И продукты VK — не исключение.


Представьте, что вы посмотрели видео про электросамокаты. Что произойдет дальше? Скорее всего, система предложит вам что-то похожее — например, обзор другого гаджета или тест-драйв электровелосипеда. Это работает благодаря **item2item-подходу**: он помогает находить похожие ролики на основе контента, тегов и описаний. По сути это быстрый способ найти «близких родственников» уже просмотренного видео.

Более сложные методы опираются не на сами видео, а на поведение аудитории. Алгоритм анализирует, какие ролики часто смотрят пользователи с похожими интересами. Если, скажем, люди, интересующиеся самокатами, часто переходят на видео о городской мобильности или экологичных технологиях, система начнет предлагать такой контент и вам. Этот подход называют **коллаборативной фильтрацией**: он строится на схожести вкусов, даже если контент сам по себе сильно различается.

А **гибридные модели** объединяют оба подхода. Они учитывают и содержимое видео, и поведение аудитории, чтобы не только показывать то, что похоже на уже просмотренное, но и находить неожиданные, но релевантные рекомендации. Именно такая комбинация лежит в основе современных рекомендательных систем.

В конечном счете долгосрочный успех любой социальной сети определяется тем, насколько ею комфортно пользоваться — как читателям, так и авторам. Сегодня рекомендательные системы — это уже не дополнение, а основа цифрового сервиса. И именно их развитие становится главным полем для экспериментов, улучшений и основой конкурентного преимущества.

https://nplus1.ru/material/2025/06/09/vk-recom - Как разработчики AI VK создают систему рекомендаций контента




ПРАКТИКА




Получение доступа к VK API

Для работы с API ВКонтакте используется механизм OAuth-авторизации, позволяющий получить access token — специальный ключ доступа к данным пользователя.

Процесс получения токена:

Формирование URL-запроса авторизации:
    

https://oauth.vk.com/authorize?client_id=2685278&display=page&redirect_uri=https://oauth.vk.com/blank.html&scope=groups&response_type=token&v=5.131

Переход по данной ссылке в браузере.
    
Подтверждение разрешения доступа к данным пользователя.
    
После авторизации происходит перенаправление на страницу:
    

https://oauth.vk.com/blank.html

В адресной строке браузера появляется параметр:

access_token=...

Данный параметр представляет собой токен доступа, который используется для выполнения запросов к API.

---

3. Настройка среды разработки

Для реализации проекта была выбрана среда программирования Python.

В рамках подготовки проекта были выполнены следующие действия:

Создана отдельная директория проекта:
    

vk_diploma

Создано виртуальное окружение Python:
    

python -m venv .venv

Активировано виртуальное окружение.
    
Установлены необходимые библиотеки:
    

pip install requests python-dotenv

Используемые библиотеки:

|Библиотека|Назначение|
|---|---|
|requests|выполнение HTTP-запросов к VK API|
|python-dotenv|загрузка конфигурации из файла .env|

---

4. Хранение конфигурации проекта

Для безопасного хранения токена доступа был использован файл конфигурации .env.

Файл содержит:

VK_TOKEN=access_token  
VK_API_VERSION=5.131

Использование .env позволяет:

не хранить токен непосредственно в коде,
    
упростить конфигурацию проекта,
    
повысить безопасность.
    

---

5. Реализация первого API-запроса

Для проверки корректности подключения был выполнен запрос метода:

users.get

Данный метод возвращает базовую информацию о пользователе.

Пример реализации запроса на Python:

import os  
import requests  
from dotenv import load_dotenv  
  
load_dotenv()  
  
TOKEN = os.getenv("VK_TOKEN")  
API_VERSION = os.getenv("VK_API_VERSION")  
  
url = "https://api.vk.com/method/users.get"  
  
params = {  
    "access_token": TOKEN,  
    "v": API_VERSION  
}  
  
response = requests.get(url, params=params)  
data = response.json()  
  
print(data)

В результате выполнения запроса был получен JSON-ответ следующего вида:

{  
  "response":   
    {  
      "id": ...,  
      "first_name": "...",  
      "last_name": "..."  
    }  
  ]  
}

Это подтвердило корректность авторизации и возможность работы с API.

---

6. Получение списка сообществ пользователя

Следующим этапом был выполнен запрос метода:

groups.get

Данный метод позволяет получить список сообществ, в которых состоит пользователь.

Пример запроса:

data = vk_call("groups.get", {  
    "extended": 1,  
    "count": 1000  
})

Результатом выполнения запроса стал JSON-объект, содержащий:

общее количество сообществ пользователя;
    
список объектов сообществ.
    

Пример структуры ответа:

{  
 "response": {  
   "count": 1,  
   "items":   
     {  
       "id": 2112240**,  
       "name": " "  
     }  
   ]  
 }  
}

---

7. Сбор данных о пользователях сообщества

Для построения рекомендательной системы необходимо получить информацию о других пользователях и их подписках.

Для этого был использован метод:

groups.getMembers

Алгоритм работы:

выбрать сообщество;
    
получить список участников;
    
сохранить идентификаторы пользователей;
    
использовать данные для дальнейшего анализа.
    

Пример запроса:

groups.getMembers

API возвращает список идентификаторов пользователей.

7.2 Входные данные

На предыдущем шаге (метод groups.getMembers) был сформирован файл:

members.txt — список идентификаторов пользователей (по одному user_id в строке), являющихся участниками исходного сообщества.
    

Также были получены подписки целевого пользователя:

my_groups — множество идентификаторов сообществ, в которых состоит целевой пользователь (в эксперименте: 1 сообщество).
    

7.3 Используемые методы VK API

В рамках этапа использовались методы:

groups.get — получение списка сообществ пользователя (по user_id);
    
groups.get (для целевого пользователя) — для исключения уже имеющихся подписок из рекомендаций;
    
groups.getById — получение названий сообществ по их идентификаторам (для интерпретации результатов).
    

7.4 Алгоритм формирования рекомендаций

Алгоритм реализован скриптом recommend_from_members.py и включает шаги:

Загрузка списка подписок целевого пользователя my_groups (для исключения уже имеющихся сообществ).
    
Чтение списка пользователей из members.txt.
    
Ограничение числа обрабатываемых пользователей параметром MAX_USERS (в эксперименте: 800).
    
Для каждого пользователя выполняется запрос groups.get.
    
    если список сообществ доступен, результат учитывается (статус ok);
        
    если недоступен (например, из-за приватности), фиксируется ошибка (статус fail).
        
Подсчёт частоты встречаемости каждого сообщества среди пользователей (только для сообществ, отсутствующих у целевого пользователя):
    
    count(g)=∑u∈UokI(g∈Gu)⋅I(g∉Gme)count(g) = \sum_{u \in U_{ok}} I(g \in G_u) \cdot I(g \notin G_{me})count(g)=u∈Uok​∑​I(g∈Gu​)⋅I(g∈/Gme​)
Сортировка сообществ по убыванию count(g) и выбор top-N кандидатов.
    
Получение названий top-сообществ методом groups.getById.
    

Для соблюдения ограничений частоты запросов к API между запросами добавлена пауза (sleep) порядка 0.35 сек.

7.5 Результаты эксперимента

В ходе запуска recommend_from_members.py были обработаны 800 пользователей из аудитории выбранного сообщества.

Промежуточные логи показывали накопление обработанных пользователей и рост числа уникальных обнаруженных сообществ. Итоговая статистика:

Users to process: 800
    
ok: 462
    
fail: 338
    
доля успешных запросов:
    
    462800≈0.5775 (≈57.8%)\frac{462}{800} \approx 0.5775 \ (\approx 57.8\%)800462​≈0.5775 (≈57.8%)
candidate groups: 72 061 (уникальные сообщества-кандидаты, отсутствующие у целевого пользователя)
    

Таким образом, около 58% пользователей предоставили данные о своих подписках, а приватность/ограничения API привели к недоступности подписок примерно у 42% пользователей. При этом объём кандидатов оказался достаточно большим для дальнейших исследований и фильтрации.

7.6 Пример top-рекомендаций (по частоте встречаемости)

Список рекомендаций формировался как наиболее часто встречающиеся сообщества среди пользователей аудитории (за исключением уже имеющихся подписок целевого пользователя). Пример top-20 (частота → название → screen_name):

111 — Интересные события в Санкт-Петербурге (blog_fiesta)
    
110 — Идеи дизайна интерьера (design2121)
    
107 — ДТП и ЧП | Санкт-Петербург | Питер Онлайн | СПб (spb_today)
    
96 — Сама Меньшова (samamenshova)
    
93 — Food.ru — про жизнь со вкусом и смыслом (foodrumedia)  
    …
    
69 — Просто Вкусно (prostovkusno_food)
    

Данный список является первичной выдачей “популярного среди аудитории” и будет дорабатываться на следующем этапе путём введения фильтров (например, отсечение слишком популярных/слишком общих сообществ) и/или учёта схожести пользователей.

---

Итог этапа 7

Реализован программный модуль сбора подписок пользователей из аудитории выбранного сообщества и построен первичный список сообществ-кандидатов для рекомендации. Получены количественные характеристики доступности данных (ok/fail) и продемонстрирован пример результата ранжирования.







АЛГОРИТМ

Группы:
![[Pasted image 20260308010725.png]]
Выполнение программы python get_my_groups.py (получение списка моих групп):
![[Pasted image 20260308011807.png]]

Открытый файл с группами, сохраненными в json:

![[Pasted image 20260308011923.png]]

Выполнение программы python collect_members.py (получение списка участников групп, в которых состоит пользователь):
![[Pasted image 20260308013058.png]]
Итого сохраненных пользователей в файле 32928
![[Pasted image 20260308013117.png]]

Содержимое файла members.txt:
![[Pasted image 20260308013419.png]]

Выполнение программы python collect_user_groups.py (получение списка групп, в которых состоят участники,):
![[Pasted image 20260308015038.png]]

Содержимое файла user_groups.json:
![[Pasted image 20260308015311.png]]

User-based Collaborative Filtering

1. Поиск похожих пользователей

Используется **коэффициент Жаккара**.

 пример

мои группы:  
A B C D

пользователь:  
A B E

пересечение

A B

объединение

A B C D E

similarity = 2/5 = 0.4

2. Выборка из 50 пользователей с максимальной похожестью
3. Формирование рекомендаций: для каждой группы считается оценка рекомендации


![[Pasted image 20260308020415.png]]
sim = jaccard(my_groups, groups)
![[Pasted image 20260308020710.png]]
